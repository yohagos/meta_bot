import { ViewportTriggerDirective } from './viewport-trigger.directive';

describe('ViewportTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewportTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
